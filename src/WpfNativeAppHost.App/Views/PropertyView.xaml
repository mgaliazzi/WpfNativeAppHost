<UserControl x:Class="WpfNativeAppHost.Views.PropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="220">
    <!-- Placeholder for whatever WPF-side UI a real shell would put beside the guest. -->
    <DockPanel Margin="8">
        <TextBlock DockPanel.Dock="Top"
                   Text="Properties"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8" />
        <TextBlock TextWrapping="Wrap"
                   Foreground="Gray"
                   Text="An ordinary WPF pane. It resizes, styles and data-binds as usual, independently of the guest application next to it." />
    </DockPanel>
</UserControl>
