<UserControl x:Class="WpfNativeAppHost.Views.HostedAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Background="#FF2D2D30">
        <!--
            The hosted window is added here at runtime. Note that once it is present it paints over
            anything else in this Grid: a native HWND is always on top of WPF content ("airspace").
        -->
        <ContentControl x:Name="HostContainer" />

        <TextBlock x:Name="StatusText"
                   Margin="24"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="#FFDCDCDC"
                   Text="Starting..." />
    </Grid>
</UserControl>
